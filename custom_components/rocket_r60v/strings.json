{
  "config": {
    "step": {
      "user": {
        "title": "Rocket R60V",
        "description": "Enter the control endpoint. Default is the Aperture bridge (nova-prospekt); use the machine's own AP (192.168.1.1) only if talking to it directly.",
        "data": {
          "host": "Host",
          "port": "Port"
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]"
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    }
  }
}
